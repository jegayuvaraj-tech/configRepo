server:
  port: 8081

spring:
  application:
    name: userService

resilience4j:
  circuitbreaker:
    instances:
      orderService:
        slidingWindowSize: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
